# Verification Matrix

| Change surface | Fast check | Broader check | Behavioral evidence | Fallback or blocker | Owner/update trigger |
| --- | --- | --- | --- | --- | --- |
| Documentation only | git diff --check and link review | make harness-check | Canonical routes resolve | Fix the named link or route | Maintainers; docs change |
| Library or core logic | swift test --filter CapabilitiesTests | make check | Capability JSON, artifact analyzers, policy gates, and toolchain status remain deterministic | Record missing Xcode/device authority as a scoped result | Maintainers; core change |
| MCP/API surface | scripts/smoke_mcp.sh; make mcp-daemon-smoke | make check | JSON-RPC initialize, tools/list, analysis calls, Simulator controls, LLDB-DAP probe transcript, authenticated loopback health, session routing, and graceful endpoint cleanup | Inspect stderr and SDK transport contract | Maintainers; tool or transport change |
| Deterministic PR tier | make pr-check | CI on every push/pull request | Core build/tests, stdio/daemon MCP smoke, macOS fixture, replay, plugin XPC, and whitespace/placeholder checks | Must not require Simulator, physical-device, or release authority | Maintainers; PR gate changes |
| Host integration tier | make host-integration-check | Manual or scheduled host integration job | Multi-client debugger ownership, daemon DELETE cleanup, replay, and plugin XPC integration | Keep fixture-only and local; do not use physical-device state as a substitute | Maintainers; daemon/session/plugin changes |
| macOS desktop backend | make mcp-mac-debug-workflow-smoke; make performance-analysis-smoke; make runtime-diagnostics-smoke; make assembler-smoke; make control-flow-smoke; make memory-map-smoke; make signing-audit-smoke; make patch-workflow-smoke; make reverse-capability-smoke; make workbench-build-smoke; make workbench-ui-smoke | make check | Real stdio MCP workflow emits bounded JSON evidence for LLDB launch, source/instruction/function/exception breakpoints, stop snapshots, forward trace, CFG/call graph, typed vmmap snapshots/diffs, xctrace rows/hotspots/flame stacks, runtime diagnostics, assembly patch/rollback, signing audit/patch planning, and policy/failure cleanup; the native Workbench builds, launches, and exposes target/evidence selection, session state, typed threads/stack/registers/stop evidence, bounded workflow manifests, and existing analyzer panels; reverse/time-travel and kernel memory remain explicit false capabilities | Keep target mutation opt-in and respect adapter capability support; the UI smoke requires an accessible local GUI session | Maintainers; backend addition |
| macOS menu bar supervisor | `make menubar-build-smoke`; `make menubar-ui-smoke`; `make mcp-daemon-smoke`; `make package` | manual signed-bundle UI check | Real `AppleDebugMenuBar.app` launches, starts the bundled authenticated daemon child, waits for health, exercises popover stop/restart/endpoint-copy/Quit cleanup, verifies menu-bar OSLog telemetry, exposes endpoint/status plus server start/stop, launch-at-login, server-at-login, Login Items guidance, log, and Quit controls, and packages both executables | Login registration requires a signed app bundle installed in a user-visible application location; daemon transport remains loopback-only and token-authenticated; UI smoke requires an accessible local GUI session | Maintainers; menu app addition |
| iOS Simulator backend | make ios-fixture-smoke; make xcode-artifact-smoke; make xcode-test-smoke; make simulator-environment-smoke; make repro-bundle-smoke; make simulator-repro-bundle-check | make ios-debug-fixture-smoke, make ios-ui-tree-smoke, and make ios-arbitrary-ui-smoke | Generic Xcode build returns app/dSYM artifacts; install/launch/screenshot, LLDB attach/inspection, project-backed/generated arbitrary-app UI, environment controls, and reproducible evidence bundle pass | Requires Xcode and, for target mutation, an available Simulator plus explicit mutation gate | Maintainers; backend addition |
| Simulator integration tier | make simulator-check; make simulator-check-core; make simulator-repro-bundle-check | Manual/scheduled run on a host with an available runtime; hosted CI executes the core and repro-bundle targets in separate jobs | Lifecycle, debugger, MCP, UI, Xcode, environment, and repro-bundle smoke suite | Do not promote to PR evidence when CoreSimulator is unavailable; keep repro-bundle isolated from the core job budget | Maintainers; Simulator/Xcode changes |
| Physical iOS backend | apple_device_list, CoreDevice process parser tests, and authorized-device suite | `make ios-coredevice-lifecycle-smoke` and `make ios-coredevice-debug-control-smoke` on a paired modern device plus `make ios-legacy-debug-control-smoke` on the iOS 15 fixture | CoreDevice tunnel refresh, install, deterministic launch/PID, process inventory/lifecycle, sysdiagnose contract, remote LLDB-DAP attach, xctrace capture, threads/stack/registers/memory/disassembly, breakpoint/control, rollback, and cleanup; legacy evidence remains separate | Requires authorized device, Developer Mode, signing, compatible Xcode/device support, and user direction; physical screenshot/UI remains a public-tooling boundary | Maintainers; device addition |
| Physical-device integration tier | make physical-device-check | Explicit manual run only | Runs the supplied legacy and/or modern physical-device control/lifecycle workflows after validating environment grants | No hosted runner or automatic fallback; missing device inputs are `blocked`/not run | Maintainers; physical-device changes |
| Compatibility baseline | docs/product-specs/compatibility-matrix.md | Current-host command output plus each tier | Separates declared macOS/Xcode/SDK floors from current-host, Simulator, and physical-device evidence | Do not claim minimum-version support from a newer host alone | Maintainers; dependency/toolchain changes |
| Security-sensitive boundary | Capability restriction tests | Target-specific policy suite | Unauthorized operation fails closed | Keep operation disabled | Maintainers; policy change |
| Codex plugin package | make codex-plugin-smoke; make package; make release-package | Repo marketplace plugin, skill, `.mcp.json`, launcher, and packaged executable | Plugin validation and stdio MCP handshake pass; package staging includes the plugin and executable | Local Apple toolchain and release signing remain separate prerequisites | Maintainers; plugin or packaging changes |
| Apple binary analysis | swift test --filter AppleBinaryDiffTests | make check | Code signature, entitlements, dependencies, symbols, dyld exports, Objective-C/Swift metadata, regular-Mach-O/.app/.dSYM diff reports, dSYM-aware symbolication, and crash-frame triage | Keep analyzers bounded and never execute artifact inputs | Maintainers; toolchain change |
| Repository harness authority | make harness-check | `python3 scripts/harness_contract.py` plus the bundled read-only validator | Current routes, managed plans, 31-row coverage/evidence shape, command registry, maintenance status, and product gate; commit-bound certification remains optional | Fail closed on native contract error; do not claim certificate freshness from local evidence | Maintainers; harness change |
| Release or production | make package; make release-package | `.github/workflows/ci.yml` plus configured release Mac | Unsigned package is CI-buildable; signed release target validates Developer ID, notarization staple, and Gatekeeper when authorized | Apple Developer credentials and explicit release authorization are required for the external submission | Human product owner; release scope |

## Rules

Use a narrow deterministic check before a broader check. Do not mark a backend verified from compilation alone. A missing real device, release credential, or production verifier is a scoped blocker and must not be replaced with a local assertion.
