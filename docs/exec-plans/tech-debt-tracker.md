# Technical Debt Tracker

| ID | Area | Evidence | Impact | Owner | Next action or revisit trigger | Status |
| --- | --- | --- | --- | --- | --- | --- |
| DEBT-001 | Modern physical iOS debugging | iPhone 17 CoreDevice tunnel refresh, install, PID-returning launch, remote LLDB-DAP control, rollback, and cleanup pass through `make ios-coredevice-debug-control-smoke`; iOS 15 remains covered through `ios-deploy` | Future devices still depend on Apple pairing, Developer Mode, signing, compatible Xcode support, and explicit grants | Apple Debug MCP maintainers | Re-run the modern target when Xcode/device support changes; retain the legacy path for iOS 15 hardware | resolved-for-current-scope |
| DEBT-002 | Static analysis metadata | Mach-O headers, signatures, Objective-C/Swift metadata, source-backed public Swift AST, deep DWARF DIE/type/source/line reports, symbolication, binary diff, and crash reports are implemented | Full AST recovery from a compiled binary still depends on compiler-private/source artifacts; source-backed AST is the truthful public boundary | Apple Debug MCP maintainers | Keep `apple_swift_ast_inspect` and `apple_dwarf_inspect` bounded; add a new contract only when a stable public format exists | resolved-for-current-scope |
| DEBT-003 | Simulator UI inspection | Project-backed/generated XCUITest probes plus bounded normalized coordinate actions inspect and act on installed apps | Full app-specific semantics still depend on XCTest accessibility exposure; coordinate actions provide input but not semantic element names | Apple Debug MCP maintainers | Keep generated probe bounded and retain project-backed fallback for app-specific test configuration | resolved-for-current-scope |
| DEBT-004 | Release engineering | `make release-package` signs, notarizes, staples, and Gatekeeper-validates the macOS app archive; CI validates unsigned packages | Hosted notarized release publishing is not automated because credentials remain local | Apple Debug MCP maintainers | Add an explicitly authorized tag/manual release workflow only when credential handling is approved | resolved-for-current-scope |
| DEBT-005 | Reverse execution | Installed Apple LLDB reports process trace start/stop but no process record, reverse-step, reverse-continue, or replay commands | x64dbg-style time-travel cannot be truthfully provided by the current public Apple backend | Apple Debug MCP maintainers | Re-run `apple_debug_reverse_capabilities` after an Apple LLDB backend exposes a public reversible process API; keep forward stop tracing meanwhile | blocked-by-platform |
| DEBT-006 | Kernel debugging | SIP/KDK/entitlement boundary prevents stock local kernel task attach/read/write and kext debugging | x64dbg kernel/driver workflows do not have a supported counterpart in this unprivileged MCP process | Apple Debug MCP maintainers | Revisit only with an explicitly authorized privileged/KDK workflow; keep `apple_kernel_capabilities` fail-closed | blocked-by-platform |
| DEBT-007 | Live dyld shared-cache tooling | `dyld_shared_cache_util` is unavailable and no mounted cache exists; direct header/table parser, selected-image Mach-O/export/nlist/chained-fixup extraction, ObjC/Swift runtime strings, pointer cross-references, and synthetic evidence are implemented | Modern optimized cache local-symbol formats may need additional public layout evidence or a mounted fixture | Apple Debug MCP maintainers | Re-run live cache smoke when a public utility or mounted cache fixture is available | resolved-for-current-scope |
| DEBT-008 | External plugin execution | Signed candidate-service audit, explicit XPC service-name transport, App Sandbox XPC protocol, timeout/payload bounds, and signed fixture smoke exist; sandbox-exec remains explicit profile-only diagnostics | Each real third-party analyzer must ship its own signed App Sandbox `.xpc` service and capability mediation; the repository fixture is not a production analyzer | Apple Debug MCP maintainers | Approve a real plugin contract and add service-specific capability mediation before distribution | resolved-for-current-scope |
| DEBT-009 | External process and DAP safety | Apple tool invocations use wall-clock deadlines, live file-backed stdout/stderr caps, file-backed stdin, bounded termination escalation, and LLDB-DAP header/body/event/stderr limits | Tool-specific Apple behavior can still change with future Xcode releases, but owned invocations now fail closed on hangs or unbounded output | Apple Debug MCP maintainers | Re-run the focused process/DAP tests after toolchain or transport changes | resolved-for-current-scope |
| DEBT-010 | MCP debugger-session ownership | Each stdio/daemon MCP server receives a distinct `ToolCatalog.Context`; daemon HTTP DELETE, expiry, startup failure, and shutdown close the context’s debugger/kernel-lab managers; concurrent clients are capped at eight | Physical-device and long-running target behavior still depends on explicit device/toolchain authorization | Apple Debug MCP maintainers | Keep two-client isolation and daemon session-limit/DELETE smoke coverage green | resolved-for-current-scope |
| DEBT-011 | MCP tool-contract evidence | A dedicated MCP test target checks unique tool names, object schemas, required-field declarations, unknown-tool failure, representative domain success/error envelopes, and per-context ownership; `mcp-domain-behavior-smoke` exercises 31 real MCP success/error cases across all dispatch domains; dispatch is split into domain files | Full behavior of every tool still belongs to its domain-specific unit or integration smoke; schema and dispatch coverage are not substitutes for backend evidence | Apple Debug MCP maintainers | Keep the matrix green and add a focused case when a new tool family or backend boundary is introduced | resolved-for-current-scope |
| DEBT-012 | Hosted Simulator CI tier | CI run `32998363346` verified Simulator video, UI, arbitrary-UI, Xcode, and environment paths but exhausted the 45-minute job budget before hosted repro-bundle output; local `make repro-bundle-smoke` passes | Hosted full-tier completion is slower and less deterministic than the local-first product requires | Apple Debug MCP maintainers | Revisit only if hosted Simulator becomes a required PR/release gate; split repro-bundle into its own job or give it a separate budget | deferred-non-blocking |
